#ifndef SIMLIB_MATH_H
#define SIMLIB_MATH_H

#include <cmath>

namespace sim
{
    int Gcd(int, int);
    int Lcm(int, int);
    int Sign(int);
    int NumberLength(int);
    int NumberPos(int, int);
}

#endif // SIMLIB_MATH_H
